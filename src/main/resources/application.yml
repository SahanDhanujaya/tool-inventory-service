spring:
  application:
    name: tool-inventory-service

  datasource:
    url: jdbc:mysql://localhost:3306/tool_library
    username: root
    password: 95334238Sd$
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

  servlet:
    context-path: /tool-inventory-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
